RewriteEngine On
RewriteBase /recetario_api/

# Si el archivo o carpeta existe, usarlo tal cual
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Si no existe, agregar .php
RewriteRule ^([^\.]+)$ $1.php [L]
